{
  "name": "quinta",
  "version": "3.17.0",
  "description": "Quinta — interactive circle of fifths, chord-progression builder and harmonic companion for musicians and producers.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node test/ci-tests.mjs && node test/smoke-controls.mjs",
    "serve": "npx serve -p 4242 dist",
    "dev": "node build.js && npx serve -p 4242 dist",
    "cap:sync": "node build.js && npx cap sync",
    "cap:ios": "node build.js && npx cap sync ios && npx cap open ios",
    "cap:android": "node build.js && npx cap sync android && npx cap open android",
    "test:controls": "node test/smoke-controls.mjs",
    "build:v2": "node build.js --v2"
  },
  "engines": {
    "node": ">=16"
  },
  "keywords": [
    "music-theory",
    "circle-of-fifths",
    "harmony",
    "producers"
  ],
  "devDependencies": {
    "esbuild": "^0.28.1",
    "playwright-core": "^1.61.0"
  }
}
